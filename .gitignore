# Hugo build output — CI rebuilds from source on every push to main.
public/
resources/_gen/
.hugo_build.lock

# LinkedIn export media that is not published. Both are local backups of the
# 2026-08-08 download, kept out of the repo on purpose:
#   _linkedin-video   — 17 native videos (187 MB), queued for YouTube upload
#   _linkedin-orphans — media whose post is absent from the Shares export
_linkedin-video/
_linkedin-orphans/

# The episode audio is NOT committed: the published page plays Mercatus's own
# file (decided 2026-08-28). This local copy is the build/alignment copy only.
static/research/*.mp3

# Python bytecode from scripts/
__pycache__/
*.pyc
